#!/bin/sh
# Global pre-push hook — blocks all pushes in this environment.
# Installed via: git config --global core.hooksPath /path/to/hooks

echo "error: pushes are not permitted in this environment." >&2
echo "       Use pull requests via the GitHub API proxy instead." >&2
exit 1
