#!/bin/sh
# Global pre-commit hook — blocks all commits in this environment.
# Installed via: git config --global core.hooksPath /path/to/hooks

echo "error: commits are not permitted in this environment." >&2
echo "       Use pull requests via the GitHub API proxy instead." >&2
exit 1
